spec:
  name: renepreact
  services:
    - build_command: yarn build
      environment_slug: node-js
      git:
        branch: main
        repo_clone_url: https://github.com/Reneuwumuhire/renepreact.git
      name: renepreact
